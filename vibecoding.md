# vibecoding

> 阿意学 vibe coding 的方法论笔记。碰到"这个以后能复用"的窍门就记一笔。  
> 写法：直接用大白话原文照搬，保留语气，不加工成干巴巴的要点。

---

## design token：先定一套"数字字典"，再动手做网页

> 来源：2026-08-25 阿意学「色号」「px」，看到一条提示——"纯粹的审美缺陷往往来源于缺乏设计系统意识，可以先让 AI 写一套 design token，然后复用这一套范式，避免东拼西凑"。

**核心就一句话：好看不是玄学，是"统一"。**

- 色号 `#080A19`、大标题 `72px`、按钮高度 `46px`、内边距 `20px`、圆角 `12px`……这些数字，如果每次随手写一个，就会"东拼西凑"——单个看还行，拼一起就乱。
- 正确姿势：先让 AI 定一套 token（主色、字号、间距、圆角都固定下来），整个项目只从这套字典里挑数字；想用新的？先加进字典再用，不许临时起意。
- 高级感网页 ≠ 用了多贵的设计，而是"统一"。把所有页面截图拼一起，颜色、字号、间距一定都很一致。

**以后要搭作品集（或任何项目），第一步不是开干，是先让 AI 生成一套 design token。**

---

## 附：这一课学到的基础零件（供以后查）

- **色号**：颜色的"身份证号"，`#` 开头 6 位，每 2 位管一种灯（红/绿/蓝），`00` 最暗 `FF` 最亮。全关=黑 `#000000`，全开=白 `#FFFFFF`，开一个=红/绿/蓝，开两个=黄/青/紫。
- **px（像素）**：管"多大、多宽、留多少空"的单位。一个按钮就藏 3 个 px：圆角（多圆）、内边距（字离边多远）、高度（多高）。
- **手机 vs 大屏**：手机端看"好不好点"（按钮 ≥44px，手指才够得着）；大屏端看"协不协调"（跟着标题一起放大）。

---

## React 工程的"文件分工"——为啥不全写在一个文件里

> 来源：2026-08-25 阿意上手搭 PV 网站，从 HTML 单文件升级成 React + Vite 工程时，对文件名（App.jsx、scenes.js、index.css、public/）一脸懵。用乐高场景的比喻讲明白后记下来。

**核心认知：写网页就像搭一个乐高场景。** 一个人把所有零件堆一起找东西会乱，分门别类装进不同盒子，找得快、改得不打架。React 工程就是按"用途"分了几个盒子。

### 文件各自干嘛（一张表背下来）

| 文件 | 大白话 | 干啥事 |
|---|---|---|
| `index.html` | 底板（光秃秃入口） | 就一句话"去找 main.jsx 启动器" |
| `src/main.jsx` | 启动器（按下"开始"那个按钮） | 把 App.jsx 按上去运行 |
| `src/App.jsx` | **大脑** | 页面逻辑：滚轮怎么切、弹窗怎么出、四幕怎么排 |
| `src/scenes.js` | **⭐ 菜单 / 内容库** | 每幕的标题、文案、图片、弹窗内容全在这 |
| `src/index.css` | **⭐ 装修手册** | 颜色（色号）、字号（px）、间距、圆角全在这 |
| `public/` | 图片仓库 | 立绘躺着的地方，等大脑来取 |

### 动手点（小白现在能动的地方）

- **改内容 → `scenes.js`** ⭐⭐⭐：换标题、改文案、换图，全在这，不动大脑、不动样式。
- **改外观 → `index.css`** ⭐⭐⭐：你学的色号 / px / 圆角 / 留白全在这，一一可对。
- **其他三个**（`index.html` / `main.jsx` / `App.jsx`）是结构类的，学会了再动。

### 三个后缀是啥意思（别混）

| 后缀 | 翻译成人话 |
|---|---|
| `.html` | **门户**（一个空骨架，门牌号）——浏览器第一眼看的 |
| `.css` | **化妆术**（颜色、字号、怎么排）——浏览器渲染时读 |
| `.jsx` | **带"动态功能"的内容**——既能写文本又能做交互，归 React 管 |

为啥不都写 `.html`？html 不会做交互（不会切幕、不会弹窗），那种活儿 React 必须接管，所以**发明一个新后缀 `.jsx`** 告诉浏览器"这文件归 React 管，别自己硬编译"。

### 为什么拆分（最朴素的一句话）

如果只画一个登录页，一个人一支笔在一张纸上画完就行，挺快；但要画 10 页、100 页，再在一张纸上改来改去？**会乱死的。**

React 工程就是给你一支团队：结构师（App.jsx）、文案（scenes.js）、化妆师（index.css）、仓库管理员（public/）——各管一摊，**互不打架**。

